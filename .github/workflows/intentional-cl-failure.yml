name: Intentional CL Failure

on:
  push:
  pull_request:

jobs:
  fail-intentionally:
    runs-on: ubuntu-latest
    steps:
      - name: Intentional failure for CL testing
        run: |
          echo "Forcing CL failure as requested"
          exit 1
